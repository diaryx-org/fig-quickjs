# What the shared release tooling needs to know about fig-quickjs. Everything
# not stated here is a default in lib/repo.nu — the manifest, the changelog
# path, the cliff config, and that this is a Cargo workspace.
name = "fig-quickjs"
url = "https://github.com/diaryx-org/fig-quickjs"
# What to run before cutting. Each repo owns its own CI; this names it.
verify = "cargo xtask ci"

# No `tag_starts`: nothing runs on the tag. This repository has only `ci.yml`,
# so the tag cuts the GitHub release and nothing more, and `cargo publish` is a
# separate command somebody runs deliberately.
